<%= form_for @tweet, url: { action: "create" }, html: { class: "nifty_form" } do |f| %>

  <div class="field">
    <%= f.label :Conteúdo %><br />
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit "Tweet" %>
  </div>
<% end %>
