$('#tweet_list').append('<%= j render @tweets %>');
<% if @posts.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @tweets %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>